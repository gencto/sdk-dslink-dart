name: dsalink

version: 2.0.0
description: >
  DSA IoT Platform - dsalink SDK for Dart. This package provides tools and libraries 
  for building IoT solutions using the Distributed Services Architecture (DSA) in Dart. 
  It includes support for secure communication, data serialization, and integration 
  with various IoT protocols and platforms.
homepage: "https://dsa.gencto.uk"
documentation: "https://iot-dsa.github.io/docs/sdks/dart/"

dependencies:
  args: ^2.7.0
  logging: ^1.3.0
  crypto: ^3.0.6
  path: ^1.9.1
  pro_mpack: ^2.0.2
  pointycastle: ^4.0.0
  json_diff: ^0.2.1
  web: ^1.1.1
  http: ^1.5.0
  synchronized: ^3.3.0+3

dev_dependencies:
  pedantic: ^1.11.1
  test: ^1.26.3
  build_runner: ^2.10.1
  build_web_compilers: ^4.3.2
  influxdb_client: ^2.11.0

environment:
  sdk: ^3.9.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
